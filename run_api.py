"""Convenience entry point — run with: python run_api.py

All startup logic lives in webdown/startup/api.py (composition root).
"""

from webdown.startup.api import run

if __name__ == "__main__":
    run()
